# 更新记录

## 0.2.2 - 2026-07-28

- 完成扩展清单、配置向导、通知、错误信息和输出日志的英文与简体中文国际化。
- 增加运行时语言包与 VS Code 模块测试替身，恢复 OpenAI 客户端单元测试覆盖。

## 0.2.1

- 调整配置向导顺序，先输入真实 API Key，再显示高级自定义请求头，避免将安全密钥与 JSON 请求头混淆。

## 0.2.0

- 提供商配置改为可直接编辑的 JSON，并提供可写入 `settings.json` 的默认 OpenAI 示例。
- API 地址支持裸域名、缺少协议、本地地址、缺少 `/v1`、自定义路径和完整 Chat Completions 地址。
- 当自动补齐的版本路径返回 HTTP 404 或 405 时，自动尝试无版本备用路径。

## 0.1.0

- 支持在资源管理器右键单个或多个文件后，基于提示词批量生成新文件。
- 支持 OpenAI Chat Completions 兼容 API、多配置档、模型切换与自定义请求头。
- API Key 使用 VS Code 安全存储，不写入设置文件。
- 支持统一、简短且体现提示词语义的输出文件名。
- 支持并发控制、取消、冲突文件名避让和失败明细输出。
